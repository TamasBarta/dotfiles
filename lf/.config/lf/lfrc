set icons
cmd open $set -f; rifle -p 0 $fx
cmd open-with ${{
    set -f
    rifle -l $fx
    read -p "Open with: " method
    rifle -p $method $fx
}}
set drawbox
map r open-with
set previewer ~/.config/lf/preview
